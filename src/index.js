const { compress, compressJson } = require('./compress');
const { decompress, decompressJson } = require('./decompress');

module.exports = {
    compress,
    compressJson,
    decompress,
    decompressJson,
};
